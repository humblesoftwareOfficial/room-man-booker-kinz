import { View, Text } from "react-native";
import React from "react";

export default function PersonalInfos({ onNext, onPrevious }) {
  return (
    <View>
      <Text>PersonalInfos</Text>
    </View>
  );
}
