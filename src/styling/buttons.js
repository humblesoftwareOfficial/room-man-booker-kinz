import { StyleSheet } from "react-native";

export const BUTTON_STYLE = StyleSheet.create({
  container_button: {
    padding: 10,
    alignItems: "center",
    justifyContent: "center",
  },
  button: {
    alignItems: "center",
  },
  textButton: {
    color: "white",
  },
});
